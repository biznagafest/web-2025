---
import type { Socials } from "../data/data.type";
interface Props {
  socials: Socials;
  size?: "sm" | "md" | "lg" | "xl" | "2xl" | "3xl" | "4xl" | "5xl" | "6xl";
}

const { socials, size = "lg" } = Astro.props;
---

<ul
  class="flex flex-row flex-wrap gap-2 md:gap-3 align-middle justify-center lg:justify-normal listnon text-lg md:text-xl lg:text-2xl text-dark-background dark:text-light-background"
>
  {
    socials?.devto && (
      <li>
        <a target="_blank" href={socials?.devto}>
          <i class:list={["ph", "ph-dev-to-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.github && (
      <li>
        <a target="_blank" href={socials?.github}>
          <i class:list={["ph", "ph-github-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.instagram && (
      <li>
        <a target="_blank" href={socials?.instagram}>
          <i class:list={["ph", "ph-instagram-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.linkedin && (
      <li>
        <a target="_blank" href={socials?.linkedin}>
          <i class:list={["ph", "ph-linkedin-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.mail && (
      <li>
        <a target="_blank" href={`mailto:${socials?.mail}`}>
          <i class:list={["ph", "ph-envelope", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.mastodon && (
      <li>
        <a target="_blank" href={socials?.mastodon}>
          <i class:list={["ph", "ph-at", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.medium && (
      <li>
        <a target="_blank" href={socials?.medium}>
          <i class:list={["ph", "ph-book-open", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.twitch && (
      <li>
        <a target="_blank" href={socials?.twitch}>
          <i class:list={["ph", "ph-twitch-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.twitter && (
      <li>
        <a target="_blank" href={socials?.twitter}>
          <i class:list={["ph", "ph-twitter-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.website && (
      <li>
        <a target="_blank" href={socials?.website}>
          <i class:list={["ph", "ph-globe", `text-${size}`]} />
        </a>
      </li>
    )
  }
  {
    socials?.youtube && (
      <li>
        <a target="_blank" href={socials?.youtube}>
          <i class:list={["ph", "ph-youtube-logo", `text-${size}`]} />
        </a>
      </li>
    )
  }
</ul>
